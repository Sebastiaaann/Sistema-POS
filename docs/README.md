# TechStock - Sistema de Inventario SaaS

Sistema de gestión de inventario multi-tenancy construido con React + Vite y Supabase.

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`

## 🚀 Integración del Sistema

Para integrar el sistema completo (logout corregido + notificaciones), sigue el plan paso a paso en [PLAN_INTEGRACION.md](PLAN_INTEGRACION.md).

## 📧 Configuración de Notificaciones por Email

Para recibir notificaciones cuando se crean organizaciones pendientes de aprobación, consulta la guía completa en [EMAIL_SETUP.md](EMAIL_SETUP.md).
